const validate = require("./validate");
const isValidId = require("./isValidId");
const authenticate = require("./authenticate");
const upload = require("./uploadAvatar");

module.exports = { validate, isValidId, authenticate, upload };
